# Dashboard Agent — Python dependencies
# Versions reflect the environment the demo was built/tested against.

deepagents>=0.6.3
langchain>=1.3.1
langchain-core>=1.4.0
langchain-anthropic>=1.4.3
langgraph>=1.2.1
langsmith>=0.8.3
pydantic>=2.13.4
python-dotenv>=1.2.2
starlette>=0.37            # the /feedback route mounted on the deployment (http.app)

# Run the deployment locally (Agent Server) + create assistants
langgraph-cli[inmem]>=0.4
langgraph-sdk>=0.1

# Tests
pytest>=8.0
